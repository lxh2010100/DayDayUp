cmake_minimum_required(VERSION 3.22.1)
project(main C)
include_directories(./include)
add_subdirectory(./src)
aux_source_directory(. SRC_LISTS)
add_executable(main ${SRC_LISTS})
target_link_libraries(main ${main} add sub)